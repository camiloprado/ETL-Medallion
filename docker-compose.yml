# One-command reviewer path:
#   docker compose up --build --abort-on-container-exit
# Re-run without rebuilding Postgres:
#   docker compose run --rm pipeline

name: br-open-data

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: br_open_data
      POSTGRES_USER: pipeline
      POSTGRES_PASSWORD: pipeline
    ports:
      - "55432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./sql/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pipeline -d br_open_data"]
      interval: 3s
      timeout: 5s
      retries: 20
      start_period: 5s

  pipeline:
    build: .
    # Host network: BrasilAPI is on Cloudflare IPv6. Many Docker bridges
    # have no IPv6 route (or broken NAT) and extract then fails. The job
    # still talks to Postgres on the published host port below.
    network_mode: host
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      BRASILAPI_BASE_URL: https://brasilapi.com.br
      DATA_DIR: /app/data
      POSTGRES_HOST: 127.0.0.1
      POSTGRES_PORT: 55432
      POSTGRES_DB: br_open_data
      POSTGRES_USER: pipeline
      POSTGRES_PASSWORD: pipeline
      POSTGRES_SSLMODE: disable
    volumes:
      - ./data:/app/data
    command: ["python", "-m", "pipeline", "run"]

volumes:
  pgdata:
